# Regex Module

A simple browser-based tool for testing regular expressions against sample text.

## Features

- Enter a regex pattern and test text
- Display matches clearly with match indices
- Show friendly errors for invalid regex input

## Usage

Open the HTML file in a browser and use the form to test your expression.
